services:
    main:
        build:
            context: .
            dockerfile: Dockerfile
        volumes:
            - .:/home/vscode/ai
        working_dir: /home/vscode/ai
        stdin_open: true
        tty: true
        stop_grace_period: 0s
